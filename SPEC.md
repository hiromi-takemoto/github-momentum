# GitHub Momentum — 仕様書

## これは何か

GitHubのリポジトリを「**累計star数**」ではなく「**今の勢い（1日あたり何star増えたか）**」で並べて見せるサイト。

既存のトレンド系ツールが見せているのは過去の合計。10年かけた24万starも今週の1万starも同じ扱いになっている。
このサイトは「今も伸びているか」だけを見る。

## 完成の定義（これができたら完成）

公開URLをブラウザで開くと、GitHubのリポジトリが「1日あたりのstar増加数」の多い順に並んだ表が1画面で表示される。
ボタンで「累計star順」に切り替えられる。更新が止まっているリポジトリには印がつく。
表のデータはGitHub Actionsが毎日自動で更新し、人が何もしなくても中身が育っていく。

---

## 作る順番（この順で作る。各段階の終わりで必ず公開できる状態にする）

### 第1段階: 集める（これ単体で完成品）
- `collect.mjs` … Node.js（外部ライブラリ禁止・標準のfetchのみ）
- GitHub Search API で `stars:>5000` を star数降順に **上位1000件**取得
  - `GET /search/repositories?q=stars:>5000&sort=stars&order=desc&per_page=100&page=1..10`
  - 認証は `GITHUB_TOKEN`（Actions が自動で渡す）を `Authorization: Bearer` で付ける
- 保存するのは1件あたり以下だけ:
  `full_name, stargazers_count, forks_count, open_issues_count, pushed_at, created_at, language, description, archived, html_url`
- 出力先:
  - `data/snapshots/YYYY-MM-DD.json` … その日の生データ（追記していく記録）
  - `data/latest.json` … サイトが読む1ファイル（下の計算結果入り）
- `.github/workflows/collect.yml` … 毎日 UTC 21:00（日本時間 朝6時）に実行し、変更をコミットする

**勢いの計算ルール**
- 記録が2日分以上ある場合: 直近7日以内で最も古いスナップショットと比較し `(今のstar - 昔のstar) / 経過日数` を「1日あたり」とする
- 記録が1日分しかない場合: 勢いは `null`。画面には数値を出さず「計測中」と出す。**推定値やダミーで埋めない**
- `latest.json` には `observed_since`（観測開始日）と `days_recorded`（記録日数）を必ず入れる

### 第2段階: 見せる
- `index.html` … **1ファイル完結・外部ライブラリ/CDN/フォント読み込み ゼロ**
- `data/latest.json` を fetch して表を描画
- 表の列: `順位 / リポジトリ名(リンク) / 言語 / 勢い(★/日) / 累計star / 状態`
- ページ上部に「観測開始日」と「記録◯日目」を必ず表示する（データが育っている事実を見せる）
- 表示件数は上位100件まで

### 第3段階: 切り替えと判定
- 並べ替えボタン: 「**勢い順**」⇄「**累計star順**」。押すと即座に並べ替わる（再読み込みなし）
- 状態欄の判定:
  - `archived: true` → `保存済み`
  - `pushed_at` が **180日以上前** → `更新停止`
  - 上記以外 → 空欄
- 状態が付いた行は視覚的に区別する（色を薄くするなど）

---

## 今回作らないもの（ここに書いていないものは作らない）

- グラフ・チャート・折れ線
- 検索窓、言語別フィルタ、絞り込み
- リポジトリ個別の詳細ページ
- ログイン、お気に入り、通知
- 1000件を超える追跡
- ダークモード切替ボタン
- 外部ライブラリ、CSSフレームワーク、ビルド作業（npm run build 等）

---

## 守ること

- `confirm()` `alert()` `prompt()` は使わない。確認が必要なら画面内にUIを作る
- 文字色と背景色のコントラスト比は **4.5以上**
- 外部ライブラリ・CDN・Webフォントは読み込まない（表示速度が売りのため）
- APIキーをコードに直接書かない（Actions の `GITHUB_TOKEN` を使う）
- データが無いときに数値をでっち上げない。「計測中」と正直に出す

## 公開先

GitHub Pages（リポジトリ `hiromi-takemoto/github-momentum`）
